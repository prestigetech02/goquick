# Aeonik font files

Place your Aeonik webfont files here so the landing page can use them:

- `Aeonik-Regular.woff2` (weight 400)
- `Aeonik-Bold.woff2` (weight 700)

If you have other weights (e.g. Medium, SemiBold), add them and we can reference them in `globals.css`.
